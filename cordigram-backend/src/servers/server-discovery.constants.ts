/** Discovery / community activation thresholds — keep in sync with web & mobile UI. */
export const SERVER_DISCOVERY_MIN_EVALUATE_MEMBERS = 2;
export const SERVER_DISCOVERY_MIN_MEMBERS = 3;
export const SERVER_DISCOVERY_MIN_AGE_MINUTES = 3;

export const SERVER_DISCOVERY_MIN_AGE_MS =
  SERVER_DISCOVERY_MIN_AGE_MINUTES * 60 * 1000;
